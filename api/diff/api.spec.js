describe('Diff API', function () {

    it('/api/diff/:name should list all diffs with the specified name in json format', function (done) {
        done();
    });

    it('/api/diff/:name/:id should return diff with the specified name and id in json format', function (done) {
        done();
    });

    it('/api/diff/:name/:id/raw should render the diff image', function (done) {
        done();
    });

});
